1.函数参数的寄存器为a0-a7, printf的13存放在a2中。
2.由代码第40行可知，编译器进行了函数内联，进而计算出f(8)+1的结果12
3.由第43和44行可知，跳转的地址为0x30+1536=0x630
4.根据jair指令，刚跳转后ra值为pc+4=0x34+4=0x38
5.输出结果为HEll0 World,若需要3对齐，则要将i设置为0x726c6400，但无需改变57616值，因为他按照的是二进制数字的读取方式。
6.y后面应该跟的是寄存器a2的值，原因是printf()格式字符串数量与不定参数数量不同，但函数执行仍在原本应该加载参数的寄存器中取值，因而第二个不定参数应存放于a2中，所以实际取值也是从寄存器a2中取值。